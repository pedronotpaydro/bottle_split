{
  id: event.id,
  name: event.name,
  hosted_by: event.user.username,
  user_id: event.user_id,
  location: event.location,
  date: event.date.strftime("%A, %d %b %Y %l:%M %p"),
  beer: event.beer.name,
  brewery: event.beer.brewery,
  style: event.beer.style,
  beer_description: event.beer.description,
  beer_id: event.beer.id,
  beer_image: event.beer.image,
  ago: time_ago_in_words(event.updated_at),

}
